<Window x:Class="Veriflow.Desktop.Views.VideoPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        Title="Video Preview" Height="360" Width="640"
        WindowStyle="ToolWindow"
        ResizeMode="CanResize"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Theme.Pro.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource Brush.Background}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        </Style>
    </Window.Style>
    
    <Grid Background="{StaticResource Brush.Panel}">
        <vlc:VideoView MediaPlayer="{Binding PreviewPlayer}"/>
    </Grid>
</Window>
